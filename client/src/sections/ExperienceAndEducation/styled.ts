import styled from "styled-components";

export const Container = styled.div`

`

export const ExperienceAndEducationContainer = styled.div`

    display: grid;
    grid-template-columns: repeat(2, 1fr);
    justify-items: center;
    row-gap: 4rem;
`